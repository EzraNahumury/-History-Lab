# History Lab — Run & Test

> Full project docs (architecture, diagrams, judging map) live in the repo-root `README.md`.
> This file is the quick "how do I run it" for the `history-lab/` build.

## TL;DR — it runs offline, today, with no Anna account

The app ships with **Mock mode ON by default**, so the *entire* loop
(scene → choose → Historian's Ruling → **Approve / Flag** → accuracy → storybook)
is deterministic and playable with zero quota and no login.

```bash
cd history-lab
npm install                    # local @anna-ai/cli — no global install needed (Node 22+)
pip install uv                 # the dev harness spawns a `uvx` python bridge (required)
npx anna-app validate --strict # schema + ACL coverage
npx anna-app dev --no-llm      # boots; dashboard at http://127.0.0.1:5180/
```

(Or `npm i -g @anna-ai/cli` once and drop the `npx`. The `package.json` also wraps
these as `npm run validate` / `npm run dev:off` / `npm run dev:mock` / `npm test`.)

Then open the app window from the dev dashboard and play. Mock mode needs no LLM, so
`--no-llm` is fine; drop it (and add `--mock-llm <fixture>` or log in) to test Live mode.

> **Verified on this machine (anna-app@0.1.30, no Anna account):**
> `npx anna-app validate --strict` → **✓ validate passed**; `npx anna-app doctor` →
> **all required checks passed**; `npx anna-app dev --no-llm` → **✓ dashboard
> http://localhost:5180/** with the `history-lab-ledger` Executa resolved
> (`bundled handles history-lab-ledger→tool-test-history-lab-ledger-12345678`, HTTP 200).
> `node tests/logic.test.mjs` passes 7/7, and the ledger answers the full stdio protocol
> (`initialize · describe · health · invoke · shutdown`).
>
> The ledger is a **wired `required_executas`** (deterministic commit via
> `anna.tools.invoke`); the UI degrades to the identical pure-JS score if the tool isn't
> running, so the demo never breaks. The `historian-gm` Skill is a **standalone Skill
> Executa** under `skills/` — the local `anna-app dev` harness only resolves runtime
> executas (python/node/go/binary), so a Skill is published on its own
> (`anna-app executa publish ./skills/historian-gm`) rather than app-bundled; its grounding
> is already active in-app via `system_prompt_addendum` + each scenario's `systemPrompt`.

## Two modes

| Mode | How | What happens |
|---|---|---|
| **Mock** (default) | "Mock mode" checkbox **checked** | Scene + ruling come from the grounded `scenarios.js`. Fully deterministic — use this for the **stage demo**. |
| **Live** | uncheck "Mock mode" | Scene + ruling are generated by `anna.agent.session` (stream) → `anna.llm.complete` (fallback). Needs a real Nexus / auth, or `anna-app dev --mock-llm fixtures/cuban-missile.jsonl`. |

The app **degrades automatically**: if a live call fails or won't parse, it falls back to the
scripted content, so the demo never breaks mid-run.

## Day-1 Go/No-Go (do this first)

Before trusting Live mode, prove the agent path in the harness (see root `README.md` →
"Day-1 Go / No-Go spikes", or use `../spike-harness.html`). If `agent.session` streaming
won't parse, ship the demo in **Mock mode** — it's a complete, honest, grounded experience.

## File map

```
manifest.json      schema-2 manifest (ui.host_api allowlist, csp_overrides, dev block)
app.json           store listing metadata
bundle/            the static-spa SPA (no build step, no CDNs)
  index.html       entry (loads ./anna-tool-ids.js then ./app.js as a module)
  anna-tool-ids.js handle -> minted tool_id map (regenerated by anna-app dev / apps push)
  styles.css
  sdk.js           connect() + host API handle (MATCH the import path to the scaffold)
  store.js         storage.get/set with etag + in-memory fallback
  agent.js         agent.session stream -> llm.complete -> scripted (graceful degradation)
  image.js         image.generate, DISPLAY-only (no fetch -> no CORS trap)
  ledger.js        anna.tools.invoke -> history-lab-ledger Executa (degrades to logic.js)
  scenarios.js     grounded scenario content (mock source)
  ui.js            DOM rendering (CSP-safe: no inline handlers/styles)
  app.js           orchestrator + turn state machine + human-review gate
package.json       anna-app scripts + local @anna-ai/cli devDep (no global install)
scenarios/         canonical scenario JSON (docs/seed)
skills/historian-gm/           declarative grounding Skill (standalone, publishable)
  SKILL.md           steers the agent's grounding + ruling format
  executa.json       skill publish metadata — publish via `anna-app executa publish ./skills/historian-gm`
executas/history-lab-ledger/   wired deterministic commit tool — required_executas +
  ledger.py          stdio plugin: initialize/describe/health/invoke/shutdown
  executa.json       publish + dev metadata + binary distribution profiles
  pyproject.toml     name/script == minted tool_id (uvx spawn contract)
  package_binary.sh  build a releasable PyInstaller binary (forum /t/140)
fixtures/          optional --mock-llm fixture (the built-in Mock mode is the real safety net)
.github/workflows/build-history-lab-ledger.yml   CI: build binaries -> GitHub Release
```

## Known things to verify in the harness (assumptions to confirm)

1. **SDK import path** in `bundle/sdk.js` matches what `anna-app dev` serves.
2. **`agent.session`** surface: `session({submode}).run({content})` vs `session.create/run`
   — `agent.js` handles both; confirm which fires.
3. **`storage.set`** arg shape (`{key,value,if_match}`) and the `precondition_failed` code.
4. **`chat.append_artifact`** renders at least a card; inline rendering is not assumed.
5. **`csp_overrides.img-src`** lets generated `<img>` URLs display in Live mode.
6. **Manifest field shapes** (`ui.views`, `default_size`) — if `extra='forbid'` rejects a
   field, copy the exact shape from a fresh `anna-app init` and re-add the `host_api` block.
